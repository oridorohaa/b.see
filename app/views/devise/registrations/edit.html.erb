<%# <h2 class="edit-user">Edit <%= resource_name.to_s.humanize %></h2>
<%= link_to :back do %>
  <i class="fa-solid fa-circle-chevron-left fa-xl"></i>
<% end %>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, required: true, autofocus: true, label: false %>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
    <% end %>

    <%= f.input :password,
                hint: "leave it blank if you don't want to change it",
                required: false,
                input_html: { autocomplete: "new-password" },
                label: false,
                placeholder: "password" %>
    <%= f.input :password_confirmation,
                label: false,
                placeholder: "password confirmation",
                required: false,
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :current_password,
                label: false,
                placeholder: "current password",
                hint: "we need your current password to confirm your changes",
                required: true,
                input_html: { autocomplete: "current-password" } %>
    <%= f.input :avatar, as: :file %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Update", class: "submit" %>
  </div>
<% end %>


<p class="link">Unhappy? <%= link_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>


<style>

.simple_form {
  color: #F5FF6D;
  border-radius: 10px;
  background: none;
  border: 1px solid #F5FF6D;
  padding: 0.375rem 0.75rem;
  font-size: 1rem;
  font-weight: 400;
  line-height: 1.5;
  width: 80%;
  margin: 20px auto;
}

.email {
  margin-top: 20px;
}

.form-control {
  background-color: #1C1C17 !important;
  border-radius: 5px;
  border-color: #F5FF6D;
  color: #E5E2DA !important;
  width: 90%;
  margin: 0 auto;
}

.form-label {
  color: #E5E2DA;
  margin-left: 5%;
}

.form-text {
  margin-left: 5%;
}

#user_password_confirmation {
  margin-bottom: 45px;
}

.submit {
  color: #1C1C17;
  background-color: #f5ff6d;
  border-radius: 10px;
  margin: 10px 0;
  margin-left: 5%;
  width: 90%;
}

.cancel-account {
  color: #f5ff6d;
  margin: -10px;
  padding: 2rem;
}

.fa-solid {
  width: 25px;
  background: #1C1C17;
  padding: 26px 20px;
  text-decoration: none;
  color: #F5FF6D;
  margin-top: 10px;
  margin-left: 10px;
}

.link {
  margin: 30px 0;
  display: flex;
  justify-content: center;
  align-items: center;
  color: #E5E2DA;
}

.link a {
  text-decoration: none;
  color: #F5FF6D;
  border: 1px solid #1C1C17;
  border-radius: 13px;
  letter-spacing: 0px;
  padding: 10px;
  font-weight: bolder;
}

</style>
